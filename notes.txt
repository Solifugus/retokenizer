
To Do Items:

--------------
Grammar Parser

A different notation is necessary for grammar parsing. Each segment is named
and defined to return with its name and each consituent part.

	// Phrases are identified in order..
	phrases = [
		{ name:'abc', segments:[], function:function(){..} },
		..
	]

	name     = // string identifier for phrase

	segments = [ '', [], ]

		-- 1 mandatory? 0 allowed? 1+ allowed?
		-- name each..
		'string'   -- accept any so named phrase (first) or token (second)
		[segments] -- accept any of specified segments

		{'name':[segments]}

	function = // function given scope common to phrases

	segments = [
		{ name:'stmt', constituents:[ 'input', iexpr ], function:{} },
		{ name:'stmt', constituents:[ 'output', oexpr ], function:{} },
		{ name:'..', constituents:[], function:{} },
	];






